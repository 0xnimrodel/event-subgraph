{
  "name": "event-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ zepgursoy/event-subgraph",
    "create-local": "graph create --node http://localhost:8020/ zepgursoy/event-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ zepgursoy/event-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zepgursoy/event-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
